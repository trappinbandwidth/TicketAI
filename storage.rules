rules_version = '2';
// Local emulator rules. Production rules are authored in the go-live phase
// alongside the real bucket layout (spec doc 09).
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}

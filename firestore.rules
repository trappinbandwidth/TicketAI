rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Local emulator baseline: authenticated access only. Real per-collection
    // rules mirror the server policy and land with the go-live phase (doc 09 §7).
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
